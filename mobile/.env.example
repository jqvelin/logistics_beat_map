# Base URL of the Nest API. Must include the /api global prefix (see backend main.ts).
# Use your machine's LAN IP instead of localhost when testing on a physical device.
EXPO_PUBLIC_API_URL=http://localhost:6767/api
